---
interface Props {
	title: string;
	designation: string;
	period: string;
}

const { title, designation, period } = Astro.props;
---

<div>
	<div class="flex flex-col border-border pt-8 pb-4 gap-1">
		<div class="flex items-center justify-center gap-2 w-fit">
			<h2 class="font-medium text-xl">{title}</h2>
			<slot name="links" />
		</div>
		<p class="text-muted">{designation} • {period}</p>
		<slot name="badges" />
	</div>
	<slot />
</div>
